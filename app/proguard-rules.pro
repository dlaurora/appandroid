# Phase 0 release rules.
#
# AGP, R8, Room, Hilt, and Compose provide consumer rules for the current
# scaffold dependencies. Add feature-specific keep rules here only when a
# future accepted phase introduces code that requires them.
